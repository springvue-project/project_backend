<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ssafy.happyhouse.model.mapper.StoreMapper">

<!-- 	<sql id="codeList">
		<if test="codeList != null">
			and typeMainCode in
			<foreach item="code" collection="codeList" open="(" close=")" separator="," >
				#{code}
			</foreach>
		</if>
	</sql> -->

<!-- 	<select id="searchByCodes" parameterType="StoreParamDto" resultType="StoreDto">
	select storeId, storename, address, lng, lat from store
	where regionCode = #{regionCode}
	<include refid="codeList"></include>
	</select> -->
	
	<select id="searchStore" parameterType="StoreParamDto" resultType="StoreDto">
		select storeId, storename, address, lng, lat
		from store
    	where (6371*acos(cos(radians(#{lat}))*cos(radians(lat))*cos(radians(lng)-radians(#{lng})) +sin(radians(#{lat}))*sin(radians(lat)))) <![CDATA[<]]> #{distance}
    	and ${codeType} = #{code}
	</select>
</mapper>